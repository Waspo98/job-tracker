services:
  job-tracker:
    build: .
    image: ${JOB_TRACKER_IMAGE:-job-tracker:local}
    restart: unless-stopped
    ports:
      - "${JOB_TRACKER_PORT:-5055}:5055"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
